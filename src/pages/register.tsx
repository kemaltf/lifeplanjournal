import React from "react";

type Props = {};

import RegisterComponent from "@/components/organism/Register";
const Register = (props: Props) => {
  return <RegisterComponent></RegisterComponent>;
};

export default Register;
